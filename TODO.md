1. 缺少MCP连接的注册，注册完成之后，应该要缓存一下，在后面用的时候直接用缓存的内容跑就行了。

现在有缓存但是具体调用逻辑还是要排查下，需要debug一下看在哪里。
┌─────────────────────────────────────────────────────────┐
│ agent 命令流程 │
├─────────────────────────────────────────────────────────┤
│ │
│ 1. 单消息模式 2. 交互式模式 │
│ ┌─▶ run_once() ┌─▶ run_interactive() │
│ │ │ ├─ bus_task (agent loop) │
│ │ │ ├─ outbound_task (消费响应) │
│ │ │ └─ 用户输入循环 │
│ ▼ ▼ │
│ 关闭 MCP 连接 关闭所有任务 │
└─────────────────────────────────────────────────────────┘
